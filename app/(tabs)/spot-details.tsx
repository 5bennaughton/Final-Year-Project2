import SpotDetails from '@/screens/SpotDetails';

// Keep spot-details inside the tabs group so the bottom nav stays visible.
// eslint-disable-next-line react/display-name
export default () => <SpotDetails />;
