import UserPage from '@/screens/User';

// Keep the user profile inside the tabs group so the bottom nav stays visible.
// eslint-disable-next-line react/display-name
export default () => <UserPage />;
